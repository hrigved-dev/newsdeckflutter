class NewsModel {
  String title;
  String description;
  String url;
  String urlToImage;
  String content;

  NewsModel(
      {this.title, this.content, this.description, this.url, this.urlToImage});
}
